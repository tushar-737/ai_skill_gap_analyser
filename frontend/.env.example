# Optional: point the frontend at a hosted API instead of the
# Vite dev-server proxy. In development you usually do NOT need
# this — Vite proxies /api to http://127.0.0.1:8000 automatically.
VITE_API_URL=
